name: molecule_scanner
channels:
  - conda-forge
dependencies:
  - python 3.9
  - notebook
  - jupyter
  - numpy
  - pandas
  - matplotlib
  - pip
  - pip:
      - hashlib
      - py2sambvca
      - joblib
